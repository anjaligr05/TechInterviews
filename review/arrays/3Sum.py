def _3sum(arr, target):
	'''sumSoFar = {}	
	for i in range(len(arr)):
		for j in range(i+1, len(arr)):
			for k in range(j+1, len(arr)):
				if target - arr[i] -  arr[j] in sumSoFar:
					return i, j, k
				else:
					sumSoFar[arr[k]] = i'''
	 i = 0
	n = len(arr)
	while i<n-2:
		
	return -1
print _3sum([3,2,1,4,5], 1)
print _3sum([3,2,1,4,5],7)
